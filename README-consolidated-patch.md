# CoachIQ single combined patch

This is a merged patch file that combines the fix work into one artifact.

Included:
- README cleanup
- remove dangerous Vercel rewrite
- remove ignored build errors in Next config
- CSS token alias / focus state improvements
- login flow improvements with password reset
- schema change for nullable unassigned athletes
- reusable backend validation helper
- insights validation
- today route N+1 reduction
- messages validation
- Strava query validation

This is still a best-effort patch bundle. I could not apply or test it directly in your repo from here because the GitHub connector is read-only in this session.
